# Task03 打卡笔记｜第三章 大语言模型基础

> 本章目标：理解大语言模型基本原理 + 完成本地模型部署并跑通代码  
> 打卡要求：本地模型部署跑通截图

**📌 笔记重点清单（提交前对照）**

- ✅ 必写：用了什么部署（Ollama + qwen3:0.6b）+ 截图证明本地跑通
- ✅ 必写：学到了什么（预测下一词 / 本地部署意义 / 幻觉）
- ✅ 建议写：怎么运用（把天气 Agent 改成离线版）
- ⚠️ 选写：原理演示代码（N_gram / BPE 跑不跑都不影响打卡）
- 截图交飞书；笔记评优可选，发 GitHub 链接即可

---

## 一、任务完成情况（本地模型部署跑通）

### 1.1 部署方案

| 项目     | 内容                                                  |
| ------ | --------------------------------------------------- |
| 推理框架   | **Ollama**（本地模型运行环境，自带 OpenAI 兼容 API）               |
| 本地服务地址 | `http://localhost:11434`                            |
| 使用模型   | **qwen3:0.6b**（约 522 MB，轻量级，可在普通电脑 CPU 运行）          |
| 调用方式   | 用 `openai` 库指向 Ollama 的兼容端点，无需安装 torch/transformers |
| 脚本位置   | `code/chapter3/local_deploy_ollama.py`              |

### 1.2 为什么用 Ollama 而不是官方 `Qwen.py`

官方 `code/chapter3/Qwen.py` 走的是 `transformers` 路线，需要：

- `pip install transformers torch`（Windows 上 torch 的 CPU 版轮子约 118 MB，不大）
- 从 HuggingFace 镜像下载约 1 GB 的模型权重（**这才是真正占时间/占网的地方**）
- CPU 推理速度慢

**Ollama 的优势**：模型已经下载在本地、开箱即用，并暴露标准 OpenAI 兼容接口（`/v1/chat/completions`），可以直接复用前面章节已经装好的 `openai` 库，**零额外依赖**。两者本质完全一样——都是"模型权重跑在自己电脑上，不联网、不调云端 API"。

### 1.3 核心代码

```python
from openai import OpenAI

# 关键：base_url 指向本机 Ollama，而不是云端服务
client = OpenAI(
    base_url="http://localhost:11434/v1",
    api_key="ollama",           # 本地服务不校验，随便填一个占位即可
)

response = client.chat.completions.create(
    model="qwen3:0.6b",
    messages=[{"role": "user", "content": "你好，请用一句话介绍你自己"}],
)

print(response.choices[0].message.content)
```

**关键点**：只要把 `base_url` 从云端地址换成本机 `http://localhost:11434/v1`，模型就从"云端大模型"变成了"跑在自己电脑上的本地模型"——代码一行都不用改结构。

### 1.4 运行步骤与结果

两种方式都跑通了，任选其一作为截图证据（**推荐方式二**，因为它同时证明了"本地模型"和"能连续对话"）。

**方式一：脚本调用（Python 用 `openai` 库指向本机 Ollama）**

```bash
cd /d D:\hello-agents-main
venv\Scripts\activate
python code/chapter3/local_deploy_ollama.py
```

运行输出：

```
正在用本地模型 qwen3:0.6b 生成回答...

模型的回答：
您好！我是AI，我专注于帮助您解决问题。我能够协助您进行各种任务，比如写作、学习或者解决问题等。请问您有什么需要帮助的事情吗？😊
```

**方式二：命令行交互模式（`ollama run`，本地跑通截图）**

```bash
cd /d D:\hello-agents-main
ollama run qwen3:0.6b
```

进入交互模式后直接提问，可连续对话。下面这次问的是 Sepang 的天气：

```
>>> can you check the weather in Malaysia, Sepang
Thinking...
Okay, the user is asking about the weather in Sepang, Malaysia, but I need to check if I can do that. First, I
should confirm that Sepang is a city in Malaysia...
...done thinking.

Sepang is a major city in Peninsular Malaysia, known for its vibrant Sepatan area. However, I currently cannot
access live weather data for Sepang. Here's a general overview of the weather in Peninsular Malaysia:

- **Seasonal Variability**: The weather varies by season. In the dry season, it may be dry and cool, while in the
  wet season, it can be rainy and warm.
- **Local Conditions**: Sepang experiences a tropical climate, with temperatures ranging from 20°C to 30°C...
- **Best Practice**: For the most accurate information, check local weather services...

Would you like a forecast for Sepang in the coming days?

>>> Send a message (/? for help)
```

![Task03 本地部署运行截图（Ollama 交互模式）](Task03_截图_交互模式.png)

> 截图说明：`ollama run qwen3:0.6b` 启动本地模型 → 提问 Sepang 天气 → 模型先输出 `Thinking...` 推理过程，再给出回答 → 末尾 `>>>` 提示符说明仍可继续对话。

✅ **本地部署跑通**：模型在本人电脑上完成推理，全程未联网调用任何云端 API。

**这次运行还顺带印证了本章两个知识点：**

1. **"能连续对话"是 Ollama 在背后拼历史** —— 末尾 `>>> Send a message` 说明还能接着问，但模型本身无记忆（呼应第一章"记忆靠历史拼接"）。
2. **幻觉的直观例子** —— 我问的是 Sepang（马来西亚雪邦），模型却把它理解成 "Peninsular Malaysia / Sepatan 区"，还自称无法联网查实时天气。这恰好说明：模型只是在"猜下一个词"、没有事实核查 → 所以第一章的天气 Agent 才必须真去调 `get_weather`。

### 1.5 关于"含金量"的诚实说明（重要）

我这次用的是 **Ollama**，没有手写官方的 `Qwen.py`（transformers 路线）。担心"没打代码、会不会不过关"——这里把判断说清楚：

- **打卡硬性要求是"本地模型部署跑通截图"**。Ollama 跑起来的模型：权重在你自己硬盘、推理在你自己 CPU、不联网、不调任何云端 API → **100% 符合"本地部署"定义**。
- 章节用 `Qwen.py`（transformers）只是**教学展示内部**（把张量、加载进度打印出来），它是"把发动机拆给你看"，不是"唯一正确答案"。两条路线对"跑通模型"这件事**完全等价**。
- **真正的学习价值不在"用哪个工具"，而在"理解本地部署意味着什么"**：模型可以不在云端、可以零 API 费用、可以离线跑。这个认知我已经通过对比两条路线想通了。
- 若想让笔记"有代码感"：1.3 那 10 行脚本我确实跑了（`local_deploy_ollama.py`），它本身就是代码；把 `base_url` 从云端换成本机 `11434` 这一行，正是"本地部署"最直观的代码体现。

> 结论：**用 Ollama 过关没问题**，重点是笔记里讲清"为什么这是本地部署、和官方路线差在哪"。

### 1.6 `ollama run` 的两种模式（踩坑记录）

| 命令                           | 模式        | 现象                           |
| ---------------------------- | --------- | ---------------------------- |
| `ollama run qwen3:0.6b "你好"` | **一次性问答** | 模型答完直接退出，无法追问（我一开始误以为"不能对话"） |
| `ollama run qwen3:0.6b`      | **交互模式**  | 进入聊天，可连续提问；输入 `/bye` 退出      |

**知识点**：本地模型本身无记忆，交互模式"能接着聊"是因为 Ollama 在背后自动把历史拼回每一轮——这正好印证第一章"记忆靠历史拼接"的结论。

---

## 二、这章我学到了什么（用大白话讲）

### 2.1 大模型到底在干嘛

一句话：**大模型就是个"猜下一个词"的机器**。  
你给它前半句，它猜"正常情况下下一个词最可能是啥"；把猜出来的词接上去，再猜下一个……就这么一直"接龙"下去，拼成了回答。

为什么能聊得像人？因为它读了全网的文本，知道"一般情况下词该怎么接"。

### 2.2 它是怎么进化来的（演进主线）

| 思路                   | 怎么干活                                 | 毛病               |
| -------------------- | ------------------------------------ | ---------------- |
| **N-gram（数词频）**      | 看前面两三个词，数哪个词常跟在后面                    | 只看一点点上下文，句子一变就傻  |
| **词嵌入**              | 把词变成"意思向量"，意思近的词在向量里靠得近（如 国王−男+女≈女王） | 只能表示"词"，还不会连成句   |
| **RNN（串着算）**         | 一个词一个词往后传，能记长一点                      | 必须排队算，慢          |
| **Transformer（注意力）** | 一句话里每个词都能同时"看"到其他词，谁相关看谁             | 又快又准，现代大模型都它     |
| **GPT（只要预测下一词）**     | 把上面砍到最简：只留"猜下一词"一个目标                 | 简单却能吃下全网，成了智能体大脑 |

> 类比帮助记：N-gram 像"只看过邻座两人"就猜全班故事；Transformer 像"全班同时传纸条"，谁和谁有关一眼看清——所以又快又准。

### 2.3 怎么跟模型"说话"（提示工程）

- **Temperature（温度）**：调低 → 回答稳、偏死板（适合问答、写代码）；调高 → 花样多（适合写诗、头脑风暴）。
- **Few-shot（给例子）**：先给模型几个示例再问，它照着学。
- **思维链 Chain-of-Thought**：让模型"一步步想"，复杂题更准。
- **分词 BPE**：模型不是按"字"也不是按"整词"看，而是拆成"子词"——既省空间，又能认出没见过的词。

### 2.4 模型的"毛病"——幻觉

模型有时会**一本正经地编瞎话**。根因：它只是在"猜下一个词"，没有"查证"这一步。  
怎么治：① 先查资料再答（RAG）；② 让它调工具（比如查天气）；③ 多想几遍再答。  
→ 这正好解释了我第一章的天气 Agent 为什么**非要真去调 `get_weather`**，而不是让模型直接编——模型本身没有事实核查能力。

### 2.5 本章和智能体开发的联系

- 提示工程 = 设计 Agent 的"思考提示"（呼应第一章 System Prompt）
- 模型选型 = 决定用云端 API 还是本地部署
- 幻觉缓解 = 给 Agent 加工具 / 检索

---

## 三、原理演示代码运行记录

第三章目录 `code/chapter3/` 下共有 5 个脚本。**打卡只要求跑通本地部署**，其余 4 个是帮助理解原理的演示，跑不跑都不影响打卡。

| 文件                       | 对应知识点                    | 依赖                                         | 能否直接跑                        |
| ------------------------ | ------------------------ | ------------------------------------------ | ---------------------------- |
| `N_gram.py`              | 3.1.1 N-gram / Bigram 概率 | 纯标准库                                       | ✅ 可直接跑                       |
| `BPE.py`                 | 3.2.2 文本分词 BPE 合并过程      | 纯标准库                                       | ✅ 可直接跑                       |
| `Word_Embedding.py`      | 3.1.1 词嵌入与余弦相似度          | 需 `numpy`                                  | ⚠️ 先装 numpy（约 20 MB）         |
| `Transformer.py`         | 3.1.2 手写 Transformer     | 需 `torch`                                  | ⚠️ 可装（约 118 MB），但纯演示，不跑不影响打卡 |
| `Qwen.py`                | 3.2.3 官方本地部署             | 需 `transformers` + `torch` + 联网下载约 1 GB 权重 | ⚠️ 需联网下载模型权重，已用 Ollama 方案替代  |
| `local_deploy_ollama.py` | 3.2.3 本地部署（我的方案）         | 已有 `openai`                                | ✅ 已跑通                        |

### 运行命令

```bash
cd /d D:\hello-agents-main
venv\Scripts\activate

# 1) N-gram：算 Bigram 概率
python code/chapter3/N_gram.py

# 2) BPE：看词表怎么合并
python code/chapter3/BPE.py

# 3) 词嵌入：需要先装 numpy
pip install numpy
python code/chapter3/Word_Embedding.py

# 4) Transformer：需要 torch（约 118 MB），纯演示
# python code/chapter3/Transformer.py
```

**各脚本预期输出（简述）**：

- `N_gram.py` → 分步打印 `P(datawhale)`、`P(agent|datawhale)`、`P(learns|agent)`，最后连乘得到整句概率。
- `BPE.py` → 每一轮打印最高频词元对及其合并结果，能看到 `h u g </w>` 逐步被合并成 `hug</w>`。
- `Word_Embedding.py` → 打印 `king - man + woman` 的结果向量，以及它与 `queen` 的余弦相似度（约 0.99）。

---

## 四、学习心得与运用

> **写作提示（给自己，不用抄进 GitHub）**
>
> 1. 不要重抄章节摘要，写"这章让我想通了什么"
> 2. 一句话点题 → 一个生活类比 → 回扣第一章
> 3. 诚实地写"在 AI 辅助下完成，并理解了其原理"
> 4. 简短为主，不堆篇幅

### 4.1 心得正文（可直接使用 / 可改成自己的话）

前面第一章我其实一直在"用"模型，却不知道它为什么能说话。第三章把这块补上了：原来大模型做的事可以压缩成一句话——**根据前面的词，预测下一个最可能的词**。

顺着这条线，很多东西一下就通了。N-gram 时代只能看前面两三个词，还全靠数词频，句子稍微一变就抓瞎；RNN 能记长一点，却必须一个字一个字串着算，慢；直到 Transformer 用自注意力让每个词能同时"看"全句，再配上位置编码补回顺序，才终于又快又准。而 GPT 干脆把编码器砍掉，只留下"预测下一个词"这一个目标——简单，却能吃下全网文本，这大概就是它能变成智能体"大脑"的原因。

更让我印象深刻的是**幻觉**这一节。我做的天气 Agent 一开始之所以需要真的调用 `get_weather` 工具去查，而不是让模型直接回答，本质就是因为模型只是在"预测下一个最可能的词"，它没有事实核查模块。课里讲的 RAG、外部工具调用，其实我第一章已经在用了，只是当时不知道背后的道理。

如果说第一章让我学会了怎么把智能体"搭起来"，那第三章让我明白了它脚下踩着的到底是什么。

### 4.2 我学到了什么（给自己理清）

1. **LLM 的本质** = 自回归的"预测下一个词"机器，不是"懂知识"的数据库。
2. **本地部署的意义** = 模型跑在自己机器上，隐私不出本机、零 API 费用、可离线。
3. **两条路线等价** = Ollama（封装好，开箱即用）和 transformers（拆开看内部）只是透明度不同，结果一样。
4. **幻觉根因** = 自回归只预测概率最高的词，没有事实核查 → 缓解靠检索(RAG) + 工具调用 + 验证。
5. **模型选型是权衡** = 不是越大越好，要在能力/成本/速度/上下文窗口/部署方式之间取舍。

### 4.3 可以怎么运用（落到自己项目）

- **把天气 Agent 改成完全离线**：把 `FirstAgentTest.py` 里的 `BASE_URL` 换成 `http://localhost:11434/v1`、`MODEL_ID` 换成 `qwen3:0.6b`、key 随便填 → 第一章的 agent 立刻变成"不联网也能跑"的本地版，正好复用了本章的部署知识。
- **隐私/离线场景优先本地小模型**：以后做涉及个人数据的功能，先想"能不能本地跑"，而不是默认调云端。
- **用采样参数控输出**：写代码/问答时把 Temperature 调低（更确定）；写诗/头脑风暴时调高（更多样）——这是和模型沟通的基本功。
- **遇幻觉就加工具**：任何"需要真实事实"的任务（查天气、算数、查最新资讯），都学第一章那样接外部工具，而不是赌模型"猜对"。

---

## 五、习题作答记录（挑重点）

> 6 道题为开放性思考题，此处记录 3 道核心题的作答要点，其余留要点备查。

### 习题 1（N-gram 与模型演进）

- `agent works` 在 Bigram 模型下的概率 = `P(agent) × P(works|agent)`，按迷你语料（`datawhale agent learns` / `datawhale agent works`）分词后代入计数即可（可直接跑 `N_gram.py` 验证）。
- **马尔可夫假设**：假设第 t 个词只依赖于前 N-1 个词，与更早的历史无关。
- **N-gram 根本局限**：① 数据稀疏——语料里没出现过的组合概率直接为 0；② 上下文窗口固定且极短，抓不住长距离依赖；③ 只统计共现频率，不具备语义泛化能力。
- **如何被克服**：RNN/LSTM 用隐藏状态把历史信息压缩传递，从而脱离固定窗口；Transformer 用自注意力让任意两词直接建立联系（长依赖不再衰减），并支持并行计算。

### 习题 2（Transformer 架构）

- **自注意力核心思想**：让序列中每个位置都能根据相关性去"加权聚合"其他所有位置的信息，从而一次性建立全局依赖。
- **为什么能并行**：RNN 的每一步计算都依赖上一步的输出，只能串行；Transformer 的自注意力是矩阵运算，同一层内所有位置可以同时计算。位置编码则把"词的先后顺序"以向量形式注入，弥补并行带来的顺序信息缺失。
- **Decoder-Only vs Encoder-Decoder**：完整结构是"先理解（编码器）再生成（解码器）"，适合翻译等任务；Decoder-Only 只保留解码器，用"预测下一个词"单一目标训练，结构与训练目标都更简单，易于规模化，且自回归模式天然契合一切生成类任务——所以成为 GPT、Llama 等主流大模型的选择。

### 习题 5（模型幻觉）

- 选择 **RAG（检索增强生成）**：在生成前先从外部知识库（文档库、网页）检索相关信息，把检索结果作为上下文一起喂给模型，让回答"有据可依"。
  - 优点：不需要重新训练模型，知识可实时更新，且能给出信息来源、便于验证。
  - 适用场景：企业知识问答、客服、论文/文档辅助阅读等对事实准确性要求高的场景。
- 其他方向：多步推理与自我验证、引入外部工具（搜索/计算器/代码解释器）、RLHF 对齐、高质量数据清洗。

### 其余题目要点备查

- **习题 3（分词的合理性 + BPE 解决了什么）**：字符粒度序列过长且语义弱；整词粒度词表爆炸且无法处理未登录词。BPE 通过高频子词合并，在"词表大小"和"序列长度"之间取得平衡，还能组合出训练时没见过的词。
- **习题 4（动手实践）**：已在本地用 Ollama 跑通 Qwen3-0.6B；采样参数对输出影响——Temperature 调低输出更稳定重复、调高更多样但可能不连贯。
- **习题 6（论文辅助阅读智能体）**：基座模型优先选**长上下文 + 强推理**的模型；长论文超窗口可用"分段摘要 + 检索(RAG)"解决；准确性要靠"引用原文出处 + 检索支撑 + 关键信息二次验证"来保证。

---

⚠️ **上传提醒**：本笔记不含任何密钥，可安全上传 GitHub。脚本与 `.env` 说明见 Task01 笔记。

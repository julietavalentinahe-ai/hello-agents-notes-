## 一、第一章学习心得

智能体不是按固定脚本执行的普通程序，给个目标 Agent会自己想办法（有自主性）, 能感知环境并通过执行器采取行动，其核心运作机制是一个循环：感知 → 思考（规划+选工具）→ 行动 → 观察，然后再回到感知。
智能体与外部交互的输出协议为Thought和 Action；行动两种，要么调用工具要吗Finish[最终答案]。


## 二、五分钟快速搭建智能体

官方库配套的 `FirstAgentTest.py` 是一个会思考、会调天气/景点工具的旅行助手。它用 ReAct 循环（Thought→Action→Observation）自主完成任务

## 三、运行截图
